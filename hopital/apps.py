from django.apps import AppConfig


class HopitalConfig(AppConfig):  # ✁EVérifie que la classe a le bon nom
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'hopital'  # ✁EVérifie que le nom est correct

